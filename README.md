# Optimizer — Update Payload

Auto-update files served to installed clients. Not the source repository.

`version.json` is the update manifest; clients compare its version on launch and
download only the listed files.
